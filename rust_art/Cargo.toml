[package]
name = "rust_art"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "art_benchmark"
harness = false

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
rand = "0.8"
